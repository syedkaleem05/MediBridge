# Leave unset for typical Vercel monorepo (same-origin /api).
# Only set when the React app is built for a domain that does NOT host /api routes.
#
# Example (split hosting):
# VITE_API_URL=https://api.example.com/api
